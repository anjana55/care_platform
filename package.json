{
  "name": "care-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w @care-platform/shared",
    "dev:api": "npm run start:dev -w @care-platform/api",
    "dev:web": "npm run dev -w @care-platform/web",
    "dev:public-web": "npm run dev -w @care-platform/public-web"
  },
  "overrides": {
    "esbuild": "^0.25.4"
  }
}
